/* Palco Coletivo - styles.css
   Cor principal: #e91c05
   Organização clara e responsiva
*/

:root{
  --primary: #e91c05;
  --bg-dark: linear-gradient(135deg,#e91c05 0%, #a51200 100%);
  --card-bg: rgba(255,255,255,0.98);
  --muted: #666;
  --maxw: 1100px;
  --radius: 12px;
  --shadow: 0 10px 30px rgba(0,0,0,0.15);
  --glass: rgba(255,255,255,0.06);
  --text-dark: #1b1b1b;
  --text-light: #ffffff;
}

/* Reset & base */
*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: Inter, -apple-system, system-ui, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
  background: var(--bg-dark);
  color: var(--text-light);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}

/* Header */
.site-header{
  padding:18px 12px;
  border-bottom: 1px solid rgba(255,255,255,0.06);
}
.header-inner{
  max-width: var(--maxw);
  margin: 0 auto;
  display:flex;
  align-items:center;
  gap:16px;
}
.logo{width:84px;height:84px;border-radius:10px;object-fit:cover;border:3px solid rgba(255,255,255,0.08)}
.title-block h1{font-size:20px;margin:0;letter-spacing:1px}
.subtitle{margin-top:6px;color:rgba(255,255,255,0.9);font-size:13px}

/* Layout container */
.container{
  max-width:var(--maxw);
  margin:28px auto;
  padding:0 18px;
}

/* Hero */
.hero{background:rgba(0,0,0,0.12);padding:18px;border-radius:12px;box-shadow:var(--shadow)}
.hero h2{margin:0 0 8px;font-size:1.5rem}
.lead{opacity:0.95;margin-bottom:12px}

/* Buttons */
.btn{
  display:inline-block;padding:10px 14px;border-radius:10px;border:0;font-weight:700;cursor:pointer;text-decoration:none;font-size:14px;
}
.btn.primary{background:var(--primary);color:#fff;box-shadow:0 6px 18px rgba(0,0,0,0.2)}
.btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.12);color: #fff}
.btn.wide{width:100%}

/* Section titles */
.section{margin-top:20px}
.section-title{font-size:1.1rem;margin-bottom:12px}

/* Carousel */
.carousel-wrap{position:relative;display:flex;align-items:center}
.carousel{
  display:flex;
  gap:16px;
  overflow-x:auto;
  padding:12px;
  scroll-snap-type:x mandatory;
  -webkit-overflow-scrolling:touch;
}
.carousel::-webkit-scrollbar{height:10px}
.carousel::-webkit-scrollbar-thumb{background:rgba(0,0,0,0.25);border-radius:10px}
.carousel .band-card{
  flex:0 0 320px;
  background:var(--card-bg);
  color:var(--text-dark);
  border-radius:var(--radius);
  padding:12px;
  scroll-snap-align:center;
  box-shadow:var(--shadow);
  display:flex;
  flex-direction:column;
  gap:10px;
  transition:transform .22s cubic-bezier(.2,.9,.2,1), box-shadow .22s;
}
.band-card img{width:100%;height:180px;object-fit:cover;border-radius:8px}
.band-card h4{margin:0;font-size:1.05rem}
.band-card p{margin:0;color:var(--muted);font-size:13px}
.band-card button{margin-top:10px}

/* Carousel arrows */
.carousel-arrow{
  background: rgba(0,0,0,0.22);
  border:0;color:#fff;font-size:26px;width:44px;height:44px;border-radius:8px;cursor:pointer;
}
.carousel-arrow.left{margin-right:8px}
.carousel-arrow.right{margin-left:8px}

/* Rules card */
.rules{padding:18px;border-radius:12px;background:rgba(0,0,0,0.14);box-shadow:var(--shadow)}
.rules ul{list-style:disc;margin-left:18px}

/* Band page */
.hidden{display:none}
.band-layout{display:grid;grid-template-columns: 360px 1fr;gap:18px;align-items:start}
.band-media img{width:100%;height:auto;border-radius:10px;box-shadow:var(--shadow)}
.support-block{margin-top:12px}
.small{font-size:13px;color:rgba(255,255,255,0.9)}
.note{opacity:0.85;font-size:12px;margin-top:6px}

/* cards on band page */
.card{background:var(--card-bg);color:var(--text-dark);padding:12px;border-radius:10px;margin-top:12px}
.video-wrapper{margin-top:8px;border-radius:10px;overflow:hidden}
.video-wrapper iframe{width:100%;height:360px;border:0}

/* Form */
.form{background:rgba(0,0,0,0.08);padding:16px;border-radius:10px;margin-top:12px}
.form-row{display:flex;flex-direction:column;margin-bottom:10px}
.form-row label{font-size:13px;margin-bottom:6px}
.form-row input,.form-row textarea{padding:10px;border-radius:8px;border:1px solid rgba(0,0,0,0.06);font-size:14px}
.form-actions{display:flex;gap:10px;align-items:center;margin-top:8px}

/* Footer */
.site-footer{margin-top:auto;padding:18px 0;text-align:center;color:rgba(255,255,255,0.85);border-top:1px solid rgba(255,255,255,0.04)}

/* Responsive */
@media (max-width: 900px){
  .band-layout{grid-template-columns:1fr;grid-auto-rows:auto}
  .carousel .band-card{flex:0 0 80%}
  .video-wrapper iframe{height:220px}
  .carousel-arrow{display:none}
}
